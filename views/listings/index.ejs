<% layout('layouts/boilerplate') %>

<h3 class="mb-4">All Listings</h3>

<div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-4">
  <% for(let listing of allListings) { %>
    <div class="col">
      <div class="card h-100 border-0 shadow-sm">
        <img src="<%= listing.image.url %>" class="card-img-top rounded-3" alt="<%= listing.title %>">
        <div class="card-body">
          <h5 class="card-title mb-1"><%= listing.title %></h5>
          <p class="card-text text-muted mb-2">₹<%= listing.price %> / night</p>
          <a href="/listings/<%= listing._id %>" class="btn btn-sm btn-main">Show More</a>
        </div>
      </div>
    </div>
  <% } %>
</div>
